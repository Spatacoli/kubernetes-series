# promtail-values.yaml
config:
  clients:
    - url: http://loki:3100/loki/api/v1/push

# Tolerations so Promtail runs on control plane nodes too
tolerations:
  - key: node-role.kubernetes.io/control-plane
    operator: Exists
    effect: NoSchedule
